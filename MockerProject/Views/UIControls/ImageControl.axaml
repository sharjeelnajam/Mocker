<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="MockerProject.Views.ImageControl"
             xmlns:vm="clr-namespace:MockerProject.ViewModels"
			 x:CompileBindings="True" x:DataType="vm:UIControlViewModel">
    <Panel Background="{Binding background}" 
           Width="{Binding width}"
           Height="{Binding height}"
           Opacity="{Binding opacity}"
		   >
        <Image Name="image" Source="/Assets/Mocker1.png"
               ToolTip.Tip="{Binding Control_Tip}"
               IsEnabled="{Binding isEnabled}"
               Width="{Binding width}"
               Height="{Binding height}"
			   Stretch="Fill" 
			   />
    </Panel>
</UserControl>
